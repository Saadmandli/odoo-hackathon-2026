# ============================================================
# VendorBridge - environment configuration
# Copy this file to ".env" and run the app:
#     Windows:  copy .env.example .env
#     Mac/Linux: cp .env.example .env
# ============================================================
# This points at the SHARED Neon cloud database (already loaded with demo data),
# so every teammate sees the same fully-populated app.
#
# SECURITY NOTE: this contains a database password. If your GitHub repo is PUBLIC,
# reset the password in the Neon dashboard after the hackathon to stay safe
# (or make the repo private).

DATABASE_URL=postgresql://neondb_owner:npg_f24jhHYoSwlR@ep-patient-dream-aq794hmp.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require
JWT_SECRET=vendorbridge-prod-secret-9f3a7c1e8b4d2056

# Optional: real invoice email. Works without these (emails log to the console).
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=
